{
  "name": "Arch Linux Dev Container",
  "dockerFile": "Dockerfile",
  "remoteUser": "dev",
  "mounts": [
    // Aqui você armazena todos os dados num arquivo/volume local
    "source=${localWorkspaceFolder}/.devcontainer/data,target=/home/dev/data,type=bind"
  ],
  "settings": {
    "terminal.integrated.shell.linux": "/bin/bash"
  },
  "postCreateCommand": "sudo pacman -Syu --noconfirm"
}
